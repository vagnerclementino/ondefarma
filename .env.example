# ==============================================
# Configuração de Ambiente - Ache uma Farmácia Popular
# ==============================================

# ----------------------------------------------
# Build Information
# ----------------------------------------------
# Hash do commit atual (gerado automaticamente no build)
# Em produção, pode ser configurado via CI/CD
NEXT_PUBLIC_BUILD_HASH=dev

# Versão da aplicação (exibida no footer)
NEXT_PUBLIC_APP_VERSION=1.0.0

# ----------------------------------------------
# Base URL
# ----------------------------------------------
# URL base da aplicação (usado no SSR para chamadas internas)
# Desenvolvimento: http://localhost:3000
# Produção: https://seu-dominio.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ----------------------------------------------
# Notas de Produção
# ----------------------------------------------
# Para deploy em produção (Vercel, Netlify, etc):
# 1. Configure NEXT_PUBLIC_BASE_URL com o domínio de produção
# 2. NEXT_PUBLIC_BUILD_HASH será configurado automaticamente pelo CI/CD
# 3. Certifique-se de que o arquivo data/pharmacies.csv está presente
# 4. Execute 'npm run build' para gerar o build otimizado
# 5. Execute 'npm start' para iniciar o servidor de produção
