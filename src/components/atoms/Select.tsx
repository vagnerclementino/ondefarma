import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select';

export { Select, SelectContent, SelectItem, SelectTrigger, SelectValue };
export type SelectProps = Record<string, never>;

export default Select;
